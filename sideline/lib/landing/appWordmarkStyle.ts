/** Shared with `LoginForm` and marketing landing — match wordmark glow in one place. */
export const appWordmarkStyle = {
  textShadow: "0px 0px 14px rgba(51, 65, 85, 0.2)",
  filter: "drop-shadow(0px 0px 8px rgba(31, 61, 53, 0.6))",
} as const;
